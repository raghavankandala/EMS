<div class="flash_container">
<%- flash.each do |name, msg| -%>
   <% if name.eql?('notice') || name.eql?('error') %>
	 <%= content_tag :div, raw(msg), :id => "flash_#{name}" %> 
   <% end %>
<%- end -%>
</div>
